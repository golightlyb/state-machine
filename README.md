Experiment in using a state macine to specify the low-level behaviour of
Graphical User Interface (GUI) elements, as opposed to a typical approach using
Object Oriented Programming.

See `src/example/`.
